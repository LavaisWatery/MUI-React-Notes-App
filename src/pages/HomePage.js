import { Box, colors, makeStyles } from "@mui/material";
import { Container } from "@mui/system";

export default function HomePage() {
    return (
        <Container>
            <Box sx={{marginTop: 5}}>
                fdsafasd
            </Box>
        </Container>
    );
}